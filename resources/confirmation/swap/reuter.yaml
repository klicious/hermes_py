default:
  header: { bid } { deal } usd {amount} mio {direction} {offer}
  body: |
    rate {rate.par}
    val {spot_date}
    delivery basis
  tail:
shinhan:
  header: { bid } { deal } usd {amount} agst krw {direction} {offer}
  body: |
    rate {rate.par}
    val {spot_date}
    delivery basis
  tail:
woori:
  header: { bid } { deal } usd {amount} agst krw {direction} {offer}
  body: |
    rate {rate.par}
    val {spot_date}
    delivery basis
  tail:

